import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.matiasgaleano.modoplayapp',
  appName: 'Modo Playa',
  webDir: 'www',
};

export default config;
